/**
 * Playwright global teardown — runs once after all tests.
 * Currently a no-op; add cleanup logic here if needed.
 */
async function globalTeardown() {
  // Placeholder for any cleanup (e.g. deleting test data created during tests).
}

export default globalTeardown;
